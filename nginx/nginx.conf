# upstream php-upstream {
#     server laravel_app1:9000;
#     server laravel_app2:9000;
#     server laravel_app3:9000;
# }

# server {
#     listen 80;
#     server_name localhost;

#     root /var/www/html/public;
#     index index.php index.html;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php-upstream;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param DOCUMENT_ROOT $document_root;
#     }

#     location /status {
#         stub_status on;
#         access_log off;
#         allow all;
#     }

#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
#     gzip_vary on;

#     server_tokens off;
# }



# # PHP upstream servers for load balancing
# upstream php-upstream {
#     server laravel_app1:9000;
#     server laravel_app2:9000;
#     server laravel_app3:9000;
# }
# # Main server block
# server {
#     listen 80;
#     server_name localhost;
#     root /var/www/html/public;
#     index index.php index.html index.htm;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php-upstream;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param DOCUMENT_ROOT $document_root;
#         fastcgi_connect_timeout 60s;
#         fastcgi_send_timeout 300s;
#         fastcgi_read_timeout 300s;
#         fastcgi_buffer_size 128k;
#         fastcgi_buffers 256 16k;
#         fastcgi_busy_buffers_size 256k;
#         fastcgi_temp_file_write_size 256k;
#     }

#     # NGINX status page for health check
#     location /status {
#         stub_status on;
#         access_log off;
#         allow all;  # Allowing all for container networking
#     }

#     # Enable Gzip compression
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
#     gzip_vary on;

#     # Remove server tokens
#     server_tokens off;
# }

# # PHP-FPM upstream server pool
# upstream php-upstream {
#     server laravel_app1:9000;
#     server laravel_app2:9000;
#     server laravel_app3:9000;
# }

# server {
#     listen 80;
#     server_name localhost;

#     root /var/www/html/public;
#     index index.php index.html index.htm;

#     # Laravel-friendly routing
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # PHP-FPM handler
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php-upstream;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param DOCUMENT_ROOT $document_root;

#         # Optional tuning parameters
#         fastcgi_connect_timeout 60s;
#         fastcgi_send_timeout 300s;
#         fastcgi_read_timeout 300s;
#         fastcgi_buffer_size 128k;
#         fastcgi_buffers 256 16k;
#         fastcgi_busy_buffers_size 256k;
#         fastcgi_temp_file_write_size 256k;
#     }

#     # Health check or status endpoint
#     location /status {
#         stub_status on;
#         access_log off;
#         allow all;  # You can restrict to a specific IP if needed
#     }

#     # Enable Gzip
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
#     gzip_vary on;

#     # Security
#     server_tokens off;
# }



# # PHP-FPM upstream server pool
# upstream php-upstream {
#     server app1:9000;
#     server app2:9000;
#     server app3:9000;
# }

# server {
#     listen 80;
#     server_name localhost;

#     root /var/www/html/public;
#     index index.php index.html index.htm;

#     # Laravel-friendly routing
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # PHP-FPM handler
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php-upstream;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param DOCUMENT_ROOT $document_root;

#         # Optional tuning parameters
#         fastcgi_connect_timeout 60s;
#         fastcgi_send_timeout 300s;
#         fastcgi_read_timeout 300s;
#         fastcgi_buffer_size 128k;
#         fastcgi_buffers 256 16k;
#         fastcgi_busy_buffers_size 256k;
#         fastcgi_temp_file_write_size 256k;
#     }

#     # Health check or status endpoint
#     location /status {
#         stub_status on;
#         access_log off;
#         allow all;
#     }

#     # Add metrics endpoint for nginx_exporter
#     location /metrics {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # Enable Gzip
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
#     gzip_vary on;

#     # Security
#     server_tokens off;
# }

upstream laravel_app {
    server app:9000;
    server app:9001;
    server app:9002;
}

proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m use_temp_path=off;

server {
    listen 80;
    server_name 192.168.1.109;
    root /var/www/html/public;
    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location /api/ {
        proxy_pass http://laravel_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Caching Configuration
        proxy_cache my_cache;
        proxy_cache_valid 200 60m;   # Cache responses with status 200 for 60 minutes
        proxy_cache_use_stale error timeout invalid_header http_500 http_502 http_503 http_504;
        add_header X-Proxy-Cache $upstream_cache_status;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass laravel_app;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;

        fastcgi_connect_timeout 60s;
        fastcgi_send_timeout 300s;
        fastcgi_read_timeout 300s;

        fastcgi_buffer_size 128k;
        fastcgi_buffers 256 16k;
        fastcgi_busy_buffers_size 256k;
        fastcgi_temp_file_write_size 256k;
    }

    location /metrics {
        proxy_pass http://laravel_app;
    }

    location ~ /\.ht {
        deny all;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_vary on;
}